# Git 常见操作

## 🧱 1. Git 基本概念（必须懂）

- **仓库（repository）**：存代码的地方。

- **工作区（working directory）**：你正在编辑的文件所在位置。

- **暂存区（stage/index）**：准备提交的文件集合。

- **提交（commit）**：一次代码快照。

简单理解：

```text
工作区 → 暂存区 → 仓库（commit）
```

---

## ⭐ 2. 初始化项目 `git init`

创建一个 Git 仓库：

```bash
git init
```

此时会出现 `.git` 文件夹，表示 Git 已托管代码。

---

## 📥 3. 克隆仓库 `git clone`

从远程仓库复制一份到本地：

```bash
git clone https://github.com/user/project.git
```

你会得到一个可编辑的本地项目。

---

## 📝 4. 查看文件变化 `git status`

查看哪些文件改了、哪些待提交：

```bash
git status
```

示例输出：

```text
modified: src/app.js
untracked files: README.md
```

---

## 📌 5. 添加到暂存区 `git add`

将文件放入“准备提交”的列表：

```bash
git add file.js
```

添加全部：

```bash
git add .
```

---

## 📦 6. 提交代码 `git commit`

提交一个版本：

```bash
git commit -m "修复登录 bug"
```

**注意：没 add 的改动不会提交！**

---

## 🧾 7. 查看提交历史 `git log`

查看谁改了什么：

```bash
git log --oneline
```

示例：

```
a1b2c3 修复登录 bug
d4e5f6 优化首页布局
```

---

## 🔄 8. 拉取远程更新 `git pull`

获取 + 合并远程代码：

```bash
git pull
```

如果冲突（conflict）会提示你手动解决。

---

## 🚀 9. 推送代码 `git push`

把本地提交推送到远程：

```bash
git push origin main
```

如果第一次推送新分支：

```bash
git push -u origin feature/login
```

---

## 🌿 10. 分支操作（核心）

### 创建分支

```bash
git branch feature/login
```

### 切换分支

```bash
git switch feature/login
```

### 创建 + 切换（推荐）

```bash
git switch -c feature/login
```

### 查看所有分支

```bash
git branch
```

---

## 🔀 11. 合并分支 `git merge`

例如把 feature/login 合并进 main：

```bash
git switch main
git merge feature/login
```

若出现冲突，Git 会标记：

```text
<<<<<< HEAD
你的改动
======
别人的改动
>>>>>> feature/login
```

手动修改后再提交即可。

---

## 🎯 12. 撤销与回退（非常重要）

### 撤销工作区修改（未 add）

```bash
git restore file.js
```

### 撤销暂存区的文件

```bash
git restore --staged file.js
```

### 回到某次 commit（不会丢历史）

```bash
git revert <commit_hash>
```

个人推荐：安全。

### 真·回退到旧版本（危险，但常见）

```bash
git reset --hard <commit_hash>
```

> 小心：回退后的提交会丢失。

---

## 💡 13. 查看某行是谁写的 `git blame`

```bash
git blame src/app.js
```

常用来“查凶手”。

---

## 🧹 14. 清理本地未跟踪文件 `git clean`

```bash
git clean -fd
```

删除所有未追踪文件与文件夹（很危险）！

---

## 📘 15. 常用工作流示例

### **日常开发流程**

```bash
git pull                         # 拉取最新代码
git switch -c feature/order-page  # 创建开发分支
# 写代码…
git add .                        # 暂存
git commit -m "新增订单页"       # 提交
git push                        # 推送
创建 PR → 代码审核 → 合并
```

---

## 🏷️ 标签

#git #版本控制 #前端基础 #知识卡片
